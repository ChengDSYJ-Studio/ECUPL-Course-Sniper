# Contributing

1. Create a focused branch.
2. Keep real credentials, cookies, endpoints, and personal information out of commits.
3. Add or update tests for every behavior change.
4. Run `python3 -m unittest discover -s tests -v` before opening a pull request.
5. Use the simulator or an explicitly authorized test environment.

